const domain = "localhost:8000"
const base_url = `http://${domain}/store/api/v1`
const product_image_url = `http://${domain}/product_image/`
const customer_image_url = `http://${domain}/image/`
export { base_url, product_image_url, customer_image_url }